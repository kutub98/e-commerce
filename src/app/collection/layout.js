export default function Collection({ children }) {
  return (
    <html lang="en">
      <body
      // className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <h1>Collection FOOTER</h1>
        {children}
        <h1>Collection Footer</h1>
      </body>
    </html>
  );
}
